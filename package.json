{
  "name": "scheduler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "bower": "^1.7.7"
  },
  "devDependencies": {
    "http-server": "^0.8.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm install && bower install",
    "start": "http-server ./public -a 0.0.0.0 -p 3000 -o"
  },
  "author": "a.lysenko",
  "license": "ISC"
}
